<!--
title=Portfolio
description=
<p>My portfolio will focus mostly on graphic design that I have done and some user interface design that is public-facing, because a lot of the work that I have done in the past is behind login screens due to membership and sensitive data issues.</p>
-->

<h2>Web</h2>
{web}
<article class="grid_4 whitepaper">
    {big_logo}<a href="{url}" title=""><img alt="project logo" src="{big_logo}" /></a>{/big_logo}
    <h3>{project}</h3>
    <figure>
        <a href="{url}" title=""><img alt="preview of website" src="{preview}" /></a>
    </figure>
    {content}
    <p>{#}</p>{/content}
</article>
{/web}

<h2>Print</h2>
{graphics}
<article class="grid_4 whitepaper">
    <h3>{project}</h3>
    <figure>
        <a href="{url}" title=""><img alt="preview of website" src="{preview}" /></a>
    </figure>
    {content}
    <p>{#}</p>{/content}
</article>
{/graphics}
